function updateValue() {
    // Obtém o valor atual do input range
    var rangeValue = document.getElementById('rangeInput').value;

    // Atualiza o parágrafo com o valor atual
    document.getElementById('rangeValue').innerText = rangeValue;
  }